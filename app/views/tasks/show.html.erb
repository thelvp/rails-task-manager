<h1>Details of your task</h1>

<h2>Task: <%= @task.title %></h2>
<ul>
  <li>Details: <%= @task.details %></li>
  <li>Completion status: <%= @task.completed? ? "✅" : "⬜️" %></li>
</ul>

<%= link_to "🗑️ Delete",
  task_path(@task),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %>
